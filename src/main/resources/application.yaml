server:
  port: 8080
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://${MCLUSTER_IP}:${MCLUSTER_PORT}/eureka
    healthcheck:
      enabled: true
spring:
  application:
    name: ali-service-taxi-service-79296566
